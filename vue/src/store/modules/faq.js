const state = {
    knpOptions:[]
  };

  const getters = {};

  const actions = {};

  const mutations = {
      setKnpOptions(state, params){
          state.knpOptions = params;
      },
  };

  export default {
    state,
    getters,
    actions,
    mutations
  };
